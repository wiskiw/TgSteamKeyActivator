import memoizee from 'memoizee';

export const memoize = memoizee;
const memoConfig = {
  length: 1,
  primitive: true
};

export const makeMemoized = fn => memoize(fn, memoConfig);

export default makeMemoized;
//# sourceMappingURL=memo.js.map