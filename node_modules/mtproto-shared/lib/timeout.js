'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.immediate = exports.smartTimeout = exports.pause = undefined;
exports.immediateWrap = immediateWrap;

var _bluebird = require('bluebird');

var _bluebird2 = _interopRequireDefault(_bluebird);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const cancelToken = Symbol('cancel token');

const timeoutRefs = new WeakSet();

const pause = exports.pause = delay => new _bluebird2.default(r => setTimeout(r, delay));

const smartTimeout = exports.smartTimeout = (fn, delay = 0, ...args) => {
  const newToken = Symbol('cancel id');
  const checkRun = () => {
    if (timeoutRefs.has(newToken)) {
      timeoutRefs.delete(newToken);
      return fn(...args);
    } else return false;
  };
  const promise = pause(delay).then(checkRun);
  //$FlowIssue
  promise[cancelToken] = newToken;
  return promise;
};

const cancel = promise => {
  if (!promise || !promise[cancelToken]) return false;
  const token = promise[cancelToken];
  return timeoutRefs.has(token) ? timeoutRefs.delete(token) : false;
};

const immediate = exports.immediate = (fn, ...args) => _bluebird2.default.resolve().then(() => fn(...args));

function immediateWrap(fn) {
  return arg => _bluebird2.default.resolve().then(() => fn(arg));
}

smartTimeout.immediate = immediate;
smartTimeout.cancel = cancel;

exports.default = smartTimeout;
//# sourceMappingURL=timeout.js.map