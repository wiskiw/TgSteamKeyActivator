import store from '../core';
import '../index.h';
import { async } from 'most-subject';
import 'most';
import Config from '../../config-provider';
import { faucetC } from '../../pull-stream';

export var rootSignal = async();
var root = rootSignal.multicast();

var isActive = root.map(state => state.active).skipRepeats();

export { isActive as active };
export var whenActive = faucetC(isActive);

export var homeDc = root.map(state => state.homeDc).skipRepeats();

export var uid = root.map(state => state.uid).skipRepeats();

export var networker = root.map(state => state.networker);

var stateModel = {
  active: isActive,
  homeDc,
  uid,
  networker
};

export default stateModel;
//# sourceMappingURL=index.js.map