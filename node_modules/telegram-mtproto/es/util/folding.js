import { Right, Left, Either } from './either';
import { Future } from 'fluture';
import 'fluture';

function folding(future) {
  //$ FlowIssue
  var fold = future.fold(Left, Right);
  return fold;
}

export function FutureFold(fn) {
  var future = Future(fn);
  var futureFold = folding(future);
  return futureFold;
}

export default folding;
//# sourceMappingURL=folding.js.map